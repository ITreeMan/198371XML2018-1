<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="speechtotext" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/LightBrown</item>
        <item name="colorPrimaryDark">@color/LightBrown</item>
        <item name="colorAccent">@color/orange</item>
        <item name="android:textColorPrimary">@color/LightBrown</item>
        <item name="android:textColorSecondary">@color/gray300</item>
        <item name="android:windowBackground">@color/White</item>
        <item name="android:textColorSecondaryInverse">@color/LightGreen</item>

    </style>

</resources>